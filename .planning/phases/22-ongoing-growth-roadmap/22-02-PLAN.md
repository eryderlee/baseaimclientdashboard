---
phase: 22-ongoing-growth-roadmap
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - components/admin/milestone-edit-table.tsx
  - app/admin/clients/[clientId]/page.tsx
autonomous: true

must_haves:
  truths:
    - "Admin sees a Growth Milestones section below the setup milestones table on the client detail page"
    - "Admin can add a custom growth milestone with a title and optional due date"
    - "Admin can remove a growth milestone from the list"
    - "Adding a growth milestone shows a success toast and the milestone appears in the list"
    - "Removing a growth milestone shows a confirmation and the milestone disappears from the list"
  artifacts:
    - path: "components/admin/milestone-edit-table.tsx"
      provides: "Growth milestones section with add form and remove buttons"
      contains: "Growth Milestones"
    - path: "app/admin/clients/[clientId]/page.tsx"
      provides: "Passes growth milestones data to MilestoneEditTable"
      contains: "growthMilestones"
  key_links:
    - from: "components/admin/milestone-edit-table.tsx"
      to: "app/admin/clients/[clientId]/actions.ts"
      via: "addGrowthMilestone and removeGrowthMilestone server action calls"
      pattern: "addGrowthMilestone|removeGrowthMilestone"
---

<objective>
Extend the admin client detail page to show growth milestones with add/remove controls, so the admin can manage the ongoing roadmap for each client.

Purpose: GROWTH-02 and GROWTH-03 require admin tooling to add and remove growth milestones. The server actions exist from Plan 01 — this plan wires them to the UI.

Output: Updated MilestoneEditTable with a Growth Milestones section, and updated client detail page passing growth milestone data.
</objective>

<execution_context>
@C:\Users\eryde\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\eryde\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-ongoing-growth-roadmap/22-RESEARCH.md
@.planning/phases/22-ongoing-growth-roadmap/22-01-SUMMARY.md
@components/admin/milestone-edit-table.tsx
@app/admin/clients/[clientId]/page.tsx
@app/admin/clients/[clientId]/actions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update admin client page to pass growth milestones</name>
  <files>app/admin/clients/[clientId]/page.tsx</files>
  <action>
1. The page currently calls `getClientWithMilestones(clientId)` which returns all milestones (both SETUP and GROWTH after Plan 01).

2. After serializing milestones, partition them into two arrays:
   - `serializedSetupMilestones`: milestones where `milestone.milestoneType === 'SETUP'`
   - `serializedGrowthMilestones`: milestones where `milestone.milestoneType === 'GROWTH'`

   Include `milestoneType` in the serialized milestone object (it's a string enum, no Date conversion needed).

3. Pass both arrays to MilestoneEditTable:
   ```tsx
   <MilestoneEditTable
     clientId={clientId}
     milestones={serializedSetupMilestones}
     growthMilestones={serializedGrowthMilestones}
   />
   ```
  </action>
  <verify>
- `npx tsc --noEmit` passes
- Grep for `growthMilestones` in the page file confirms the prop is passed
  </verify>
  <done>
- Admin client detail page partitions milestones by milestoneType
- Both setup and growth milestone arrays are passed to MilestoneEditTable
  </done>
</task>

<task type="auto">
  <name>Task 2: Add growth milestones section to MilestoneEditTable</name>
  <files>components/admin/milestone-edit-table.tsx</files>
  <action>
1. Add a new optional prop `growthMilestones` to the component's props interface. Type it as the same serialized milestone array type already used for `milestones`, but with `milestoneType` added.

2. Import `addGrowthMilestone` and `removeGrowthMilestone` from `@/app/admin/clients/[clientId]/actions`.

3. Below the existing setup milestones table, add a "Growth Milestones" section (only rendered when `growthMilestones` is defined — it will be undefined for the old callers until Plan 01 runs):

   Section structure:
   - A heading: "Growth Milestones" with a subtitle "Ongoing monthly review roadmap"
   - An add form: inline form with a text input for title, an optional date input for due date, and an "Add" button. Use `useTransition` for the submit (consistent with existing patterns in this component). On success, show toast via Sonner ("Growth milestone added"). On error, show error toast.
   - A list of growth milestones: render each as a row with:
     - Title
     - Due date (formatted nicely, or "No date")
     - Status badge (same styling as setup milestones)
     - A red "Remove" button (Trash2 icon from lucide-react)
   - Remove handler: call `removeGrowthMilestone(clientId, milestoneId)`. Use `useTransition`. On success, toast "Milestone removed". On error, toast the error message.

4. Add an empty state when there are no growth milestones: "No growth milestones yet. Complete all setup phases to auto-generate the monthly roadmap, or add one manually above."

5. Style consistently with the existing milestone table: use the same Card/table patterns, glass-card borders, and Tailwind classes already in the component.

Key implementation notes:
- The add form should reset after successful submission (clear title input, clear date input).
- The remove button should use `window.confirm('Remove this growth milestone?')` before calling the action (matches the existing cancel confirmation pattern from Phase 10).
- Use `isPending` from `useTransition` to disable the add/remove buttons during submission.
  </action>
  <verify>
- `npx tsc --noEmit` passes
- Grep for `Growth Milestones` in milestone-edit-table.tsx confirms the section header exists
- Grep for `addGrowthMilestone` in milestone-edit-table.tsx confirms the action is wired
- Grep for `removeGrowthMilestone` in milestone-edit-table.tsx confirms the action is wired
  </verify>
  <done>
- MilestoneEditTable renders a Growth Milestones section below setup milestones
- Admin can add a growth milestone with title and optional due date
- Admin can remove a growth milestone with confirmation
- Toast feedback on add/remove success and error
- Empty state message shown when no growth milestones exist
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Admin client page passes growthMilestones prop
- MilestoneEditTable has Growth Milestones section with add form and remove buttons
- Actions are wired: addGrowthMilestone and removeGrowthMilestone imported and called
</verification>

<success_criteria>
- Admin can see growth milestones section on client detail page
- Admin can add a custom growth milestone with title and optional due date
- Admin can remove a growth milestone with confirmation dialog
- Toast notifications confirm add/remove operations
- Empty state explains how growth milestones are generated
</success_criteria>

<output>
After completion, create `.planning/phases/22-ongoing-growth-roadmap/22-02-SUMMARY.md`
</output>
