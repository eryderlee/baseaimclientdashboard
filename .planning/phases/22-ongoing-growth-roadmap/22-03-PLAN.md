---
phase: 22-ongoing-growth-roadmap
plan: 03
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - app/dashboard/page.tsx
  - components/dashboard/dashboard-overview.tsx
  - components/dashboard/growth-roadmap.tsx
autonomous: true

must_haves:
  truths:
    - "Client whose setup is incomplete sees the existing setup phase cards (unchanged behavior)"
    - "Client whose setup is complete sees the growth roadmap section instead of setup phase cards"
    - "Growth roadmap displays monthly review cards ordered by their order field"
    - "Each growth milestone card shows title, due date, and status"
    - "Empty growth state shows a message when setup is complete but no growth milestones exist yet"
  artifacts:
    - path: "components/dashboard/growth-roadmap.tsx"
      provides: "GrowthRoadmap component rendering monthly review cards"
      contains: "GrowthRoadmap"
    - path: "app/dashboard/page.tsx"
      provides: "Fetches setup and growth milestones separately, computes setupComplete, passes to DashboardOverview"
      contains: "getSetupMilestones"
    - path: "components/dashboard/dashboard-overview.tsx"
      provides: "Conditional rendering: setup roadmap vs growth roadmap based on setupComplete prop"
      contains: "setupComplete"
  key_links:
    - from: "app/dashboard/page.tsx"
      to: "lib/dal.ts"
      via: "getSetupMilestones and getGrowthMilestones calls"
      pattern: "getSetupMilestones|getGrowthMilestones"
    - from: "components/dashboard/dashboard-overview.tsx"
      to: "components/dashboard/growth-roadmap.tsx"
      via: "conditional render when setupComplete is true"
      pattern: "setupComplete.*GrowthRoadmap"
---

<objective>
Update the client dashboard home page to conditionally show the ongoing growth roadmap (monthly review cards) instead of setup phase cards when setup is complete.

Purpose: GROWTH-04 requires clients to see a different view once their setup is done. The data layer exists from Plan 01 — this plan wires it to the client-facing UI.

Output: New GrowthRoadmap component, updated DashboardOverview with conditional rendering, and updated dashboard page fetching typed milestones.
</objective>

<execution_context>
@C:\Users\eryde\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\eryde\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-ongoing-growth-roadmap/22-RESEARCH.md
@.planning/phases/22-ongoing-growth-roadmap/22-01-SUMMARY.md
@app/dashboard/page.tsx
@components/dashboard/dashboard-overview.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update dashboard page to fetch typed milestones</name>
  <files>app/dashboard/page.tsx</files>
  <action>
1. Import `getSetupMilestones` and `getGrowthMilestones` from `@/lib/dal` (in addition to the existing imports).

2. Replace `getMilestones()` in the Promise.all with `getSetupMilestones()` and `getGrowthMilestones()`. The destructured result changes from `milestones` to `setupMilestones` and `growthMilestones`.

3. Compute `setupComplete` in the server component:
   ```typescript
   const setupComplete =
     setupMilestones.length >= 6 &&
     setupMilestones.every((m) => m.status === 'COMPLETED')
   ```

4. Serialize both arrays using the same serialization logic currently applied to `milestones` (spread, Date-to-ISO conversion, notes normalization). Apply to both `setupMilestones` and `growthMilestones`.

5. Update the DashboardOverview props:
   - Rename `milestones` prop to `setupMilestones` (passing serialized setup milestones)
   - Add `growthMilestones` prop (passing serialized growth milestones)
   - Add `setupComplete` boolean prop

6. The existing milestone-derived data (overallProgress, completedMilestones count, etc.) should still be computed from setupMilestones only — the progress stats reflect the setup journey, not the growth phase.
  </action>
  <verify>
- `npx tsc --noEmit` passes
- Grep for `getSetupMilestones` in page.tsx confirms the new DAL call
- Grep for `setupComplete` in page.tsx confirms the computation
  </verify>
  <done>
- Dashboard page fetches setup and growth milestones separately
- setupComplete is computed server-side from setup milestones
- Both milestone arrays and setupComplete are passed to DashboardOverview
  </done>
</task>

<task type="auto">
  <name>Task 2: Create GrowthRoadmap component + conditional rendering</name>
  <files>components/dashboard/growth-roadmap.tsx, components/dashboard/dashboard-overview.tsx</files>
  <action>
1. Create `components/dashboard/growth-roadmap.tsx` as a client component ("use client"):

   Props: `milestones: SerializedMilestone[]` (same type from dashboard-overview.tsx, but with milestoneType added)

   Render a card grid of monthly review milestones. Each card:
   - Title (e.g., "Monthly Review — April 2026")
   - Due date formatted as "Apr 1, 2026" or "No date set"
   - Status badge with the same getPhaseTheme color coding used in dashboard-overview.tsx (extract or duplicate the helper)
   - Progress indicator if IN_PROGRESS

   Layout: Horizontal scrollable row of cards (matching the existing setup roadmap scroll pattern at line 348-349 of dashboard-overview.tsx), OR a responsive grid `grid-cols-2 sm:grid-cols-3 lg:grid-cols-4` — use the grid approach since 12 cards are too many for a single horizontal scroll.

   Card styling: Match the existing phase card styling from dashboard-overview.tsx (rounded-3xl border, shadow-sm, hover:shadow-md transition, theme-based coloring).

   Empty state: When milestones array is empty, show a centered message: "Your growth roadmap will appear here once monthly reviews are generated." Use the glass-card dashed border empty state pattern from Phase 13 (rounded-2xl border border-dashed border-white/70 bg-white/60 p-6 text-center).

2. Update `components/dashboard/dashboard-overview.tsx`:

   a. Update the props interface:
      - Rename `milestones` to `setupMilestones`
      - Add `growthMilestones: SerializedMilestone[]`
      - Add `setupComplete: boolean`
      - Add `milestoneType?: string` to the SerializedMilestone interface

   b. Update all internal references from `milestones` to `setupMilestones` (the variable used for overallProgress, completedMilestones, orderedMilestones, currentPhase, etc.).

   c. Import GrowthRoadmap from `./growth-roadmap`.

   d. In the Growth Roadmap section (around line 316-390), wrap the content in a conditional:
      ```tsx
      {setupComplete ? (
        <>
          {/* Updated header for growth phase */}
          <CardHeader>
            <CardTitle className="font-heading text-2xl">Ongoing Growth</CardTitle>
            <CardDescription>Monthly performance reviews and optimization sessions.</CardDescription>
          </CardHeader>
          <CardContent>
            {/* Keep the phase completion stats but show growth progress */}
            <div className="grid gap-4 sm:grid-cols-2">
              <div className="...">
                <p>Reviews Completed</p>
                <p>{completedGrowth}/{totalGrowth}</p>
              </div>
              <div className="...">
                <p>Next Review</p>
                <p>{nextGrowthMilestone title or "All complete"}</p>
              </div>
            </div>
            <GrowthRoadmap milestones={growthMilestones} />
          </CardContent>
        </>
      ) : (
        // existing setup roadmap JSX (CardHeader "Growth Roadmap", phase cards, etc.)
        // Keep this EXACTLY as-is
      )}
      ```

   e. Compute growth stats from the growthMilestones prop:
      - `completedGrowth`: count of milestones with status 'COMPLETED'
      - `totalGrowth`: growthMilestones.length
      - `nextGrowthMilestone`: first milestone with status !== 'COMPLETED', ordered by order

   IMPORTANT: Do NOT change any of the existing setup roadmap rendering. The conditional branch for `!setupComplete` should contain the exact same JSX that exists today. Only the `setupComplete === true` branch is new.
  </action>
  <verify>
- `npx tsc --noEmit` passes
- File `components/dashboard/growth-roadmap.tsx` exists
- Grep for `setupComplete` in dashboard-overview.tsx confirms the conditional
- Grep for `GrowthRoadmap` in dashboard-overview.tsx confirms the import and render
- Grep for `setupMilestones` in dashboard-overview.tsx confirms the renamed prop
  </verify>
  <done>
- GrowthRoadmap component renders monthly review cards in a responsive grid
- DashboardOverview conditionally renders growth roadmap when setupComplete is true
- Setup roadmap rendering is unchanged for non-complete clients
- Growth stats (completed count, next review) are shown in the growth section header
- Empty state handled when setupComplete but no growth milestones exist
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm run build` succeeds (or `next build`)
- Dashboard page fetches typed milestones and computes setupComplete
- GrowthRoadmap component exists and renders milestone cards
- DashboardOverview shows setup phase cards when setupComplete is false
- DashboardOverview shows growth roadmap when setupComplete is true
</verification>

<success_criteria>
- Client with incomplete setup sees the existing setup phase cards (no regression)
- Client with complete setup sees the ongoing growth roadmap section
- Growth roadmap shows 12 monthly review cards with title, date, and status
- Growth section header shows progress stats (completed/total, next review)
- Empty state shown if setup complete but no growth milestones generated yet
</success_criteria>

<output>
After completion, create `.planning/phases/22-ongoing-growth-roadmap/22-03-SUMMARY.md`
</output>
