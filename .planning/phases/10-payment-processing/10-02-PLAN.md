---
phase: 10-payment-processing
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - app/admin/clients/[clientId]/invoices/page.tsx
  - app/admin/clients/[clientId]/invoices/new/page.tsx
  - components/admin/create-invoice-form.tsx
  - app/admin/clients/[clientId]/page.tsx
autonomous: true

must_haves:
  truths:
    - "Admin can see list of invoices for a specific client with status badges"
    - "Admin can create a new invoice with line items, description, and due date"
    - "Admin can navigate to invoice management from the client detail page"
  artifacts:
    - path: "app/admin/clients/[clientId]/invoices/page.tsx"
      provides: "Admin invoice list page for a client"
      min_lines: 40
    - path: "app/admin/clients/[clientId]/invoices/new/page.tsx"
      provides: "Admin new invoice creation page"
      min_lines: 20
    - path: "components/admin/create-invoice-form.tsx"
      provides: "Invoice creation form with dynamic line items"
      min_lines: 80
  key_links:
    - from: "components/admin/create-invoice-form.tsx"
      to: "app/actions/billing.ts"
      via: "createInvoice server action call"
      pattern: "createInvoice"
    - from: "app/admin/clients/[clientId]/invoices/page.tsx"
      to: "lib/dal.ts"
      via: "getAdminClientInvoices DAL function"
      pattern: "getAdminClientInvoices"
---

<objective>
Build admin UI for managing client invoices: a list page showing all invoices for a client, and a creation form that syncs to Stripe.

Purpose: Admin needs to create and manage invoices per client. This wires the admin-facing UI to the billing server actions from Plan 01.
Output: Admin invoice list page, invoice creation page with dynamic line items form.
</objective>

<execution_context>
@C:\Users\eryde\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\eryde\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-payment-processing/10-RESEARCH.md
@.planning/phases/10-payment-processing/10-01-SUMMARY.md
@lib/dal.ts
@app/actions/billing.ts
@app/admin/clients/[clientId]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin invoice list and creation pages</name>
  <files>app/admin/clients/[clientId]/invoices/page.tsx, app/admin/clients/[clientId]/invoices/new/page.tsx, components/admin/create-invoice-form.tsx</files>
  <action>
**app/admin/clients/[clientId]/invoices/page.tsx** — Server component:
- Import `getAdminClientInvoices` and `getAdminClientForBilling` from DAL (use the functions created in Plan 01)
- Await params to get clientId
- Fetch invoices and client data
- Display page title: "{companyName} — Invoices" with a "Create Invoice" button linking to `./invoices/new`
- Show invoices in a Table (use existing shadcn Table components) with columns: Invoice #, Description, Amount (formatted with currency), Due Date, Status (Badge with color: PAID=default/green, SENT=secondary/blue, OVERDUE=destructive/red, DRAFT=outline, CANCELLED=outline), Created date
- Empty state: CreditCard icon + "No invoices yet" + Create Invoice button
- Each row: link invoice number to the Stripe hosted invoice page (fetch URL on-demand via client-side action or just show as text — admin doesn't need to pay)
- Back link to `/admin/clients/${clientId}`

**components/admin/create-invoice-form.tsx** — Client component ('use client'):
- Props: `{ clientId: string }`
- Use react-hook-form with Zod validation matching the createInvoice action schema
- Fields:
  - Description (text input)
  - Due Date (date input, default 30 days from now)
  - Currency (select: usd, eur, gbp — default usd)
  - Line Items: dynamic array of { description: string, amount: number }
    - "Add Line Item" button appends new row
    - Each row has description input, amount input (number, step 0.01), and remove button
    - At least 1 line item required
  - Show total amount calculated from line items
- On submit: call `createInvoice` server action with form data
- Show loading state on submit button (useTransition)
- On success: show toast via sonner ("Invoice created and sent"), redirect to invoices list via router.push
- On error: show toast with error message
- Import createInvoice from '@/app/actions/billing'

**app/admin/clients/[clientId]/invoices/new/page.tsx** — Server component:
- Import `getAdminClientForBilling` from DAL to get client name for page title
- Await params to get clientId
- Display: "New Invoice for {companyName}" heading
- Render `<CreateInvoiceForm clientId={clientId} />`
- Back link to invoices list

Follow existing admin page patterns: use Card layout, consistent heading styles, breadcrumb-style back links.
  </action>
  <verify>
Run `npx tsc --noEmit` — no type errors. Verify the pages render without import errors by checking that all component imports resolve.
  </verify>
  <done>
Admin can navigate to /admin/clients/[clientId]/invoices to see invoice list, click "Create Invoice" to access form with dynamic line items, submit to create Stripe-synced invoice. Form validates inputs and shows success/error feedback.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add invoices link to admin client detail page</name>
  <files>app/admin/clients/[clientId]/page.tsx</files>
  <action>
Read the existing client detail page at `app/admin/clients/[clientId]/page.tsx`.

Add a navigation link/button to the invoices page. Following the existing pattern (there should be links to edit, documents, etc.):
- Add a "Invoices" link/button that navigates to `/admin/clients/${clientId}/invoices`
- Use the Receipt or CreditCard icon from lucide-react
- Place it alongside existing action buttons (Edit, Documents, etc.)
- If the page uses a card-based layout for actions, add an "Invoices" card. If it uses buttons, add an "Invoices" button.

Keep changes minimal — just add the navigation entry, don't restructure the page.
  </action>
  <verify>
Run `npx tsc --noEmit` — no type errors. Verify the invoices link appears in the client detail page source.
  </verify>
  <done>
Admin client detail page has a clear "Invoices" navigation entry leading to /admin/clients/[clientId]/invoices.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. Invoice list page renders table with correct columns
3. Create invoice form has dynamic line items with add/remove
4. Form calls createInvoice server action
5. Client detail page links to invoices
</verification>

<success_criteria>
- Admin can view all invoices for a client at /admin/clients/[clientId]/invoices
- Admin can create new invoice with line items at /admin/clients/[clientId]/invoices/new
- Invoice creation form validates inputs, calls server action, shows feedback
- Client detail page links to invoices page
- Covers STRIPE-04 (admin creates invoices) from the UI side
</success_criteria>

<output>
After completion, create `.planning/phases/10-payment-processing/10-02-SUMMARY.md`
</output>
