---
phase: 12-production-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/dashboard/loading.tsx
  - app/dashboard/analytics/loading.tsx
  - app/dashboard/billing/loading.tsx
  - app/dashboard/progress/loading.tsx
  - app/dashboard/documents/loading.tsx
  - app/dashboard/settings/loading.tsx
  - app/dashboard/chat/loading.tsx
  - app/admin/loading.tsx
  - app/admin/clients/[clientId]/loading.tsx
  - app/dashboard/error.tsx
  - app/admin/error.tsx
  - app/error.tsx
autonomous: true

must_haves:
  truths:
    - "All dashboard pages show skeleton loading states during data fetch"
    - "All admin pages show skeleton loading states during data fetch"
    - "Errors in dashboard pages show user-friendly error message with retry button"
    - "Errors in admin pages show user-friendly error message with retry button"
    - "Root-level errors show error message (not white screen)"
  artifacts:
    - path: "app/dashboard/loading.tsx"
      provides: "Dashboard overview loading skeleton"
      contains: "Skeleton"
    - path: "app/dashboard/analytics/loading.tsx"
      provides: "Analytics page loading skeleton"
      contains: "Skeleton"
    - path: "app/dashboard/billing/loading.tsx"
      provides: "Billing page loading skeleton"
      contains: "Skeleton"
    - path: "app/dashboard/progress/loading.tsx"
      provides: "Progress page loading skeleton"
      contains: "Skeleton"
    - path: "app/dashboard/documents/loading.tsx"
      provides: "Documents page loading skeleton"
      contains: "Skeleton"
    - path: "app/dashboard/settings/loading.tsx"
      provides: "Settings page loading skeleton"
      contains: "Skeleton"
    - path: "app/dashboard/chat/loading.tsx"
      provides: "Chat page loading skeleton"
      contains: "Skeleton"
    - path: "app/admin/loading.tsx"
      provides: "Admin dashboard loading skeleton"
      contains: "Skeleton"
    - path: "app/admin/clients/[clientId]/loading.tsx"
      provides: "Client detail loading skeleton"
      contains: "Skeleton"
    - path: "app/dashboard/error.tsx"
      provides: "Dashboard error boundary"
      contains: "use client"
    - path: "app/admin/error.tsx"
      provides: "Admin error boundary"
      contains: "use client"
    - path: "app/error.tsx"
      provides: "App-level error boundary"
      contains: "use client"
  key_links:
    - from: "app/dashboard/loading.tsx"
      to: "components/ui/skeleton.tsx"
      via: "import"
      pattern: 'import.*Skeleton.*from.*components/ui/skeleton'
    - from: "app/dashboard/error.tsx"
      to: "reset()"
      via: "onClick handler"
      pattern: "onClick.*reset"
---

<objective>
Add loading states (skeleton screens) to all pages and error boundaries to all route segments.

Purpose: Users currently see nothing during data fetch (no loading.tsx files) and get white screens on errors (no error.tsx files). This plan addresses PROD-02 (loading states) and PROD-07 (error boundaries).
Output: 9 loading.tsx files and 3 error.tsx files covering all dashboard and admin routes.
</objective>

<execution_context>
@C:\Users\eryde\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\eryde\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-production-hardening/12-RESEARCH.md

@components/ui/skeleton.tsx
@app/dashboard/page.tsx
@app/dashboard/analytics/page.tsx
@app/dashboard/billing/page.tsx
@app/dashboard/progress/page.tsx
@app/dashboard/documents/page.tsx
@app/dashboard/settings/page.tsx
@app/dashboard/chat/page.tsx
@app/admin/page.tsx
@app/admin/clients/[clientId]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create loading.tsx skeleton screens for all routes</name>
  <files>
    app/dashboard/loading.tsx
    app/dashboard/analytics/loading.tsx
    app/dashboard/billing/loading.tsx
    app/dashboard/progress/loading.tsx
    app/dashboard/documents/loading.tsx
    app/dashboard/settings/loading.tsx
    app/dashboard/chat/loading.tsx
    app/admin/loading.tsx
    app/admin/clients/[clientId]/loading.tsx
  </files>
  <action>
    Create loading.tsx files for each route. Each file imports Skeleton from "@/components/ui/skeleton" and renders a skeleton layout that roughly matches the shape of the actual page content.

    For EACH loading.tsx, read the corresponding page.tsx first to understand its layout shape, then create a skeleton that mirrors it.

    General patterns:
    - `app/dashboard/loading.tsx`: 4 stat cards row (grid-cols-2 lg:grid-cols-4 skeleton h-28), then chart area (skeleton h-64), then content section (skeleton h-48)
    - `app/dashboard/analytics/loading.tsx`: Metric cards row (grid-cols-2 md:grid-cols-3, 6 skeletons h-24), then chart placeholder (skeleton h-80)
    - `app/dashboard/billing/loading.tsx`: Page title skeleton, then invoice list (4-5 skeleton rows h-16)
    - `app/dashboard/progress/loading.tsx`: Page title skeleton, then milestone items (6 skeleton rows h-20 with left icon placeholder)
    - `app/dashboard/documents/loading.tsx`: Page title skeleton, upload area placeholder (skeleton h-32 border-dashed), then file list (4 skeleton rows h-14)
    - `app/dashboard/settings/loading.tsx`: Page title skeleton, then form area (3 input skeletons h-10 with label skeletons h-4, button skeleton)
    - `app/dashboard/chat/loading.tsx`: Simple centered content skeleton (skeleton h-48 max-w-md mx-auto)
    - `app/admin/loading.tsx`: Summary cards row (4 skeletons h-28), then filter bar (skeleton h-10), then table (skeleton h-96)
    - `app/admin/clients/[clientId]/loading.tsx`: Client name skeleton (h-8 w-48), action buttons (3 skeletons h-10 inline), then tabs area (skeleton h-64)

    All loading.tsx files are default-exported React components (NOT "use client" -- they are server components). Wrap in a container div with `space-y-6 p-6` or matching the page's padding pattern.
  </action>
  <verify>
    - All 9 loading.tsx files exist
    - Each imports Skeleton from "@/components/ui/skeleton"
    - None use "use client" directive (loading.tsx should be server components)
    - `npx tsc --noEmit` passes
  </verify>
  <done>Every dashboard and admin route has a loading.tsx that shows skeleton UI during data fetch, matching the approximate shape of the actual page content</done>
</task>

<task type="auto">
  <name>Task 2: Create error.tsx boundaries for dashboard, admin, and app root</name>
  <files>
    app/error.tsx
    app/dashboard/error.tsx
    app/admin/error.tsx
  </files>
  <action>
    Create 3 error.tsx files. All MUST have "use client" directive (Next.js requirement for error boundaries).

    Each error.tsx follows this pattern:
    - Props: `{ error: Error & { digest?: string }; reset: () => void }`
    - `useEffect` logs error to console: `console.error("Error:", error)` (Sentry auto-captures via onRequestError in instrumentation.ts for server errors; client errors captured by Sentry's global handler)
    - Renders centered layout with:
      - Icon or emoji indicating error (use a simple SVG circle-x or just text)
      - "Something went wrong" heading (text-lg font-semibold)
      - Error message in muted text (error.message or fallback "An unexpected error occurred")
      - "Try again" button calling `reset()` -- use Tailwind classes matching the project's Button style (bg-primary text-primary-foreground rounded-md px-4 py-2)
      - "Go back" link using Next.js Link to go to the appropriate route (/ for app, /dashboard for dashboard, /admin for admin)

    For `app/dashboard/error.tsx`:
    - Include a link to "/dashboard" as fallback navigation
    - Min height: min-h-[400px]

    For `app/admin/error.tsx`:
    - Include a link to "/admin" as fallback navigation
    - Min height: min-h-[400px]

    For `app/error.tsx`:
    - Include a link to "/" as fallback navigation
    - This catches errors that escape dashboard/admin boundaries
    - Min height: min-h-[50vh]
  </action>
  <verify>
    - All 3 error.tsx files exist
    - Each has "use client" directive as first line
    - Each imports useEffect from "react"
    - Each renders a reset button and a navigation link
    - `npx tsc --noEmit` passes
  </verify>
  <done>Dashboard errors, admin errors, and app-level errors all show user-friendly error message with retry button and navigation fallback instead of white screen</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes without errors
2. All 9 loading.tsx files exist and import Skeleton
3. All 3 error.tsx files exist with "use client" and reset() button
4. No loading.tsx has "use client" directive
5. All error.tsx files have "use client" directive
6. `npx next build` succeeds (or SKIP_ENV_VALIDATION=1 npx next build if env vars not yet set)
</verification>

<success_criteria>
- PROD-02: All pages display skeleton loading states during data fetching
- PROD-07: Error boundaries catch and display user-friendly error messages with retry + navigation
</success_criteria>

<output>
After completion, create `.planning/phases/12-production-hardening/12-02-SUMMARY.md`
</output>
