---
phase: 12-production-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - instrumentation.ts
  - instrumentation-client.ts
  - sentry.server.config.ts
  - sentry.edge.config.ts
  - app/global-error.tsx
  - lib/env.ts
autonomous: true
user_setup:
  - service: sentry
    why: "Error monitoring"
    env_vars:
      - name: SENTRY_DSN
        source: "Sentry Dashboard -> Project Settings -> Client Keys (DSN)"
      - name: NEXT_PUBLIC_SENTRY_DSN
        source: "Same DSN as SENTRY_DSN"
      - name: SENTRY_ORG
        source: "Sentry Dashboard -> Organization Settings -> slug"
      - name: SENTRY_PROJECT
        source: "Sentry Dashboard -> Project Settings -> slug"
      - name: SENTRY_AUTH_TOKEN
        source: "Sentry Dashboard -> Settings -> Auth Tokens -> Create New Token (org:read, project:releases, project:write)"

must_haves:
  truths:
    - "Sentry SDK initializes on server, client, and edge runtimes when DSN is set"
    - "Security headers (CSP, HSTS, X-Frame-Options, etc.) are returned on all responses"
    - "Application fails to build if required env vars are missing"
    - "Production build rejects Stripe test keys (sk_test_)"
  artifacts:
    - path: "instrumentation.ts"
      provides: "Sentry server/edge registration + onRequestError export"
      contains: "Sentry.captureRequestError"
    - path: "instrumentation-client.ts"
      provides: "Sentry client-side initialization"
      contains: "Sentry.init"
    - path: "sentry.server.config.ts"
      provides: "Sentry server config with DSN and trace sample rate"
      contains: "Sentry.init"
    - path: "sentry.edge.config.ts"
      provides: "Sentry edge runtime config"
      contains: "Sentry.init"
    - path: "app/global-error.tsx"
      provides: "Root-level error boundary reporting to Sentry"
      contains: "Sentry.captureException"
    - path: "lib/env.ts"
      provides: "Type-safe env validation with Zod schemas"
      contains: "createEnv"
    - path: "next.config.ts"
      provides: "withSentryConfig wrapper + security headers"
      contains: "withSentryConfig"
  key_links:
    - from: "instrumentation.ts"
      to: "sentry.server.config.ts"
      via: "dynamic import in register()"
      pattern: 'import.*sentry\.server\.config'
    - from: "next.config.ts"
      to: "lib/env.ts"
      via: "top-level import triggers validation"
      pattern: 'import.*lib/env'
    - from: "next.config.ts"
      to: "@sentry/nextjs"
      via: "withSentryConfig wrapper"
      pattern: "withSentryConfig"
---

<objective>
Set up Sentry error monitoring, configure security headers, and add environment variable validation.

Purpose: These three concerns (PROD-01, PROD-06, PROD-08) all converge on next.config.ts and form the foundation of production hardening. Sentry captures errors, security headers protect against common web attacks, and env validation prevents misconfiguration.
Output: Sentry SDK files, global-error.tsx, lib/env.ts, and next.config.ts with withSentryConfig + headers.
</objective>

<execution_context>
@C:\Users\eryde\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\eryde\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-production-hardening/12-RESEARCH.md

@next.config.ts
@middleware.ts
@app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and create Sentry configuration files</name>
  <files>
    instrumentation.ts
    instrumentation-client.ts
    sentry.server.config.ts
    sentry.edge.config.ts
    app/global-error.tsx
    next.config.ts
  </files>
  <action>
    1. Install packages: `npm install @sentry/nextjs @t3-oss/env-nextjs`

    2. Create `sentry.server.config.ts` (project root):
       - Import `* as Sentry from "@sentry/nextjs"`
       - `Sentry.init({ dsn: process.env.SENTRY_DSN, tracesSampleRate: process.env.NODE_ENV === "production" ? 0.1 : 1.0, environment: process.env.NODE_ENV })`

    3. Create `sentry.edge.config.ts` (project root):
       - Same pattern as server config but for edge runtime
       - `Sentry.init({ dsn: process.env.SENTRY_DSN, tracesSampleRate: 0.1, environment: process.env.NODE_ENV })`

    4. Create `instrumentation.ts` (project root):
       - `export async function register()` that dynamically imports server or edge config based on `process.env.NEXT_RUNTIME`
       - `export const onRequestError = Sentry.captureRequestError` for automatic server error capture

    5. Create `instrumentation-client.ts` (project root):
       - Initialize Sentry client-side with `process.env.NEXT_PUBLIC_SENTRY_DSN`
       - Include `Sentry.replayIntegration()` with `replaysOnErrorSampleRate: 1.0, replaysSessionSampleRate: 0.1`
       - Export `onRouterTransitionStart = Sentry.captureRouterTransitionStart`

    6. Create `app/global-error.tsx`:
       - "use client" component
       - `useEffect` calls `Sentry.captureException(error)` on error change
       - Renders full `<html><body>` with "Something went wrong" message and "Try again" button calling `reset()`
       - Styled with Tailwind: centered layout, text-lg heading, muted description, primary button

    7. Modify `next.config.ts`:
       - Add `import { withSentryConfig } from "@sentry/nextjs"`
       - Keep ALL existing config (serverExternalPackages, typedRoutes, turbopack)
       - Add `async headers()` function with security headers (see Task 2 action)
       - Wrap export: `export default withSentryConfig(nextConfig, { org: process.env.SENTRY_ORG, project: process.env.SENTRY_PROJECT, silent: !process.env.CI })`

    IMPORTANT: Do NOT add `experimental.instrumentationHook` -- it is NOT needed in Next.js 16 and causes warnings.
    IMPORTANT: Do NOT set `autoInstrumentServerFunctions` -- it's a no-op with Turbopack.
  </action>
  <verify>
    - `npx tsc --noEmit` passes (type check)
    - All 4 Sentry files exist at project root
    - `app/global-error.tsx` exists and contains "use client"
    - `next.config.ts` contains `withSentryConfig` and `headers()`
  </verify>
  <done>Sentry SDK is configured for server/client/edge, global error boundary captures and reports errors, next.config.ts wraps with withSentryConfig</done>
</task>

<task type="auto">
  <name>Task 2: Add security headers and environment variable validation</name>
  <files>
    next.config.ts
    lib/env.ts
  </files>
  <action>
    1. In `next.config.ts` `headers()` function, return headers for source `"/(.*)"`:
       - Build CSP string with environment awareness:
         ```
         const isDev = process.env.NODE_ENV === "development";
         default-src 'self';
         script-src 'self' 'unsafe-inline'${isDev ? " 'unsafe-eval'" : ""};
         style-src 'self' 'unsafe-inline';
         img-src 'self' blob: data:;
         font-src 'self';
         object-src 'none';
         base-uri 'self';
         form-action 'self';
         frame-ancestors 'none';
         connect-src 'self' https://*.sentry.io;
         upgrade-insecure-requests
         ```
       - X-Frame-Options: DENY
       - X-Content-Type-Options: nosniff
       - Referrer-Policy: strict-origin-when-cross-origin
       - Permissions-Policy: camera=(), microphone=(), geolocation=(), browsing-topics=()
       - Strict-Transport-Security: max-age=63072000; includeSubDomains; preload
       - X-DNS-Prefetch-Control: on

    2. Create `lib/env.ts` using `@t3-oss/env-nextjs`:
       - server vars: DATABASE_URL (z.string().url()), AUTH_SECRET (z.string().min(1)), STRIPE_SECRET_KEY (z.string().startsWith("sk_") with .refine() that rejects sk_test_ in production), STRIPE_WEBHOOK_SECRET (z.string().startsWith("whsec_")), RESEND_API_KEY (z.string().min(1)), RESEND_FROM_EMAIL (z.string().email().optional()), GOOGLE_OAUTH_CLIENT_ID (z.string().min(1)), GOOGLE_OAUTH_CLIENT_SECRET (z.string().min(1)), GOOGLE_OAUTH_REFRESH_TOKEN (z.string().min(1)), GOOGLE_DRIVE_ROOT_FOLDER_ID (z.string().min(1)), UPSTASH_REDIS_REST_URL (z.string().url()), UPSTASH_REDIS_REST_TOKEN (z.string().min(1)), SENTRY_DSN (z.string().url().optional()), SENTRY_ORG (z.string().optional()), SENTRY_PROJECT (z.string().optional()), SENTRY_AUTH_TOKEN (z.string().optional())
       - client vars: NEXT_PUBLIC_APP_URL (z.string().url()), NEXT_PUBLIC_SENTRY_DSN (z.string().url().optional())
       - Map all vars in runtimeEnv object
       - Add `skipValidation: !!process.env.SKIP_ENV_VALIDATION` for CI flexibility

    3. Add `import "./lib/env"` at top of `next.config.ts` to trigger validation at build time.

    IMPORTANT: Do NOT import lib/env.ts in middleware.ts -- edge runtime has limited module support. Use process.env directly in middleware.
    IMPORTANT: Stripe key refine should WARN (console.warn) in development, THROW only in production. Use: `.refine((key) => process.env.NODE_ENV !== "production" || key.startsWith("sk_live_"), { message: "Production requires live Stripe key (sk_live_)" })`
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - `lib/env.ts` exists and exports `env` object
    - `next.config.ts` imports `./lib/env` at top
    - Security headers are defined in `headers()` function
    - Run `SKIP_ENV_VALIDATION=1 npx next build` to verify build works (skip env check since Upstash/Sentry vars not set locally yet)
  </verify>
  <done>Security headers configured on all routes. Env vars validated with Zod at build time. Stripe test key detection works for production. Build succeeds with SKIP_ENV_VALIDATION flag.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes without errors
2. `next.config.ts` contains: withSentryConfig wrapper, headers() with CSP + HSTS + X-Frame-Options, import of ./lib/env
3. All Sentry config files exist: instrumentation.ts, instrumentation-client.ts, sentry.server.config.ts, sentry.edge.config.ts
4. app/global-error.tsx exists with "use client" directive and Sentry.captureException
5. lib/env.ts validates all known env vars with appropriate Zod schemas
6. Build succeeds: `SKIP_ENV_VALIDATION=1 npx next build`
</verification>

<success_criteria>
- PROD-01: Sentry SDK is configured for all runtimes, ready to track errors when DSN is provided
- PROD-06: Security headers (CSP, HSTS, X-Frame-Options, nosniff, Permissions-Policy, Referrer-Policy) are set on all responses
- PROD-08: Environment variables are validated at build time; Stripe test keys are rejected in production
</success_criteria>

<output>
After completion, create `.planning/phases/12-production-hardening/12-01-SUMMARY.md`
</output>
