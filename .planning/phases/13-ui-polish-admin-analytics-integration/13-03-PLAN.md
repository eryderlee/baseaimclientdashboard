---
phase: 13-ui-polish-admin-analytics-integration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - components/dashboard/dashboard-overview.tsx
autonomous: true

must_haves:
  truths:
    - "Recent Documents section shows a polished empty state when no documents exist (icon + message)"
    - "Recent Activity section shows a polished empty state when no activities exist (icon + message)"
    - "Empty states use the glass-card dashed border style consistent with the Growth Roadmap empty state"
    - "Loading animations use smooth Tailwind transitions (no jarring page shifts)"
  artifacts:
    - path: "components/dashboard/dashboard-overview.tsx"
      provides: "Polished empty states for Recent Documents and Recent Activity sections"
      contains: "No documents yet"
  key_links:
    - from: "components/dashboard/dashboard-overview.tsx"
      to: "empty state pattern"
      via: "border-dashed glass-card style"
      pattern: "border-dashed"
---

<objective>
Add polished empty states to dashboard list views and refine loading transitions throughout the dashboard.

Purpose: Fulfills UI-05 (loading animations polished) and UI-06 (empty states for all list views). The billing page already has an empty state for invoices. The NotificationCenter already has one. This plan addresses the remaining gaps in dashboard-overview.tsx and ensures transitions are smooth.

Output: All list views display meaningful empty states instead of blank space. Transitions are smooth.
</objective>

<execution_context>
@C:\Users\eryde\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\eryde\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-ui-polish-admin-analytics-integration/13-RESEARCH.md

Key files to read before implementing:
@components/dashboard/dashboard-overview.tsx
@app/dashboard/billing/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add empty states to dashboard overview sections</name>
  <files>components/dashboard/dashboard-overview.tsx</files>
  <action>
In `components/dashboard/dashboard-overview.tsx`, the `documents` and `activities` arrays are hardcoded as empty (lines 87-89). The existing code maps over them but renders nothing since they're empty. Add polished empty states.

1. Import `Clock` from lucide-react (add to existing import — `FileText` is already imported).

2. **Recent Documents section** (around line 401): After `<CardContent className="space-y-3">`, wrap the existing `documents.map(...)` in a conditional. When `documents.length === 0`, show the empty state:
   ```tsx
   <CardContent className="space-y-3">
     {documents.length === 0 ? (
       <div className="rounded-2xl border border-dashed border-white/70 bg-white/60 p-6 text-center dark:border-slate-800 dark:bg-slate-900/50">
         <FileText className="mx-auto h-10 w-10 mb-3 text-slate-300 dark:text-slate-600" />
         <p className="text-sm font-medium text-slate-500 dark:text-slate-400">No documents yet</p>
         <p className="mt-1 text-xs text-slate-400 dark:text-slate-500">
           Your team will share files here once your project kicks off.
         </p>
       </div>
     ) : (
       documents.map((doc) => (
         /* ... existing doc rendering unchanged ... */
       ))
     )}
   </CardContent>
   ```

3. **Recent Activity section** (around line 434): Same pattern. When `activities.length === 0`, show the empty state:
   ```tsx
   <CardContent className="space-y-3">
     {activities.length === 0 ? (
       <div className="rounded-2xl border border-dashed border-white/70 bg-white/60 p-6 text-center dark:border-slate-800 dark:bg-slate-900/50">
         <Clock className="mx-auto h-10 w-10 mb-3 text-slate-300 dark:text-slate-600" />
         <p className="text-sm font-medium text-slate-500 dark:text-slate-400">No recent activity</p>
         <p className="mt-1 text-xs text-slate-400 dark:text-slate-500">
           Activity updates will appear here as your project progresses.
         </p>
       </div>
     ) : (
       activities.slice(0, 4).map((activity) => (
         /* ... existing activity rendering unchanged ... */
       ))
     )}
   </CardContent>
   ```

4. **Add smooth transition classes** to the main cards in dashboard-overview.tsx for polish:
   - On the hero section `<section>` tag, add `transition-all duration-300` to the existing className
   - On each stat card in the `heroStats.map()`, add `transition-all duration-200 hover:shadow-lg` to the existing className
   - On the Growth Roadmap phase cards in `orderedMilestones.map()`, add `transition-all duration-200 hover:shadow-md` to the existing card className

These are additive CSS classes that enhance polish without changing layout or functionality.

**Do NOT modify:**
- The Growth Roadmap timeline (manual 2026-02-21 changes)
- The milestone calculations
- The analytics overview
- The hero CTA buttons
  </action>
  <verify>
Run `npm run build` — no TypeScript errors or build failures. Verify that the empty state divs contain `border-dashed` class and appropriate icon + message text.
  </verify>
  <done>
Recent Documents section shows "No documents yet" with FileText icon when empty. Recent Activity section shows "No recent activity" with Clock icon when empty. Both use the glass-card dashed border style. Dashboard cards have smooth hover transitions.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with no errors
2. Dashboard overview shows polished empty states for Recent Documents and Recent Activity sections
3. Empty states match the glass-card dashed border style used by the Growth Roadmap empty state
4. Billing page already has empty state for invoices (verify no regression)
5. NotificationCenter already has empty state (verify no regression)
6. Hover transitions are smooth on stat cards and phase cards
</verification>

<success_criteria>
- UI-05: Loading animations and transitions polished throughout dashboard (hover transitions added)
- UI-06: Empty states designed for all list views (documents, activity sections now have empty states; billing and notifications already had them)
</success_criteria>

<output>
After completion, create `.planning/phases/13-ui-polish-admin-analytics-integration/13-03-SUMMARY.md`
</output>
