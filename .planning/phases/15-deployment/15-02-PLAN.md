---
phase: 15-deployment
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified: []
autonomous: false

user_setup:
  - service: supabase
    why: "Production PostgreSQL database"
    env_vars:
      - name: DATABASE_URL
        source: "Supabase -> Project Settings -> Database -> Connection string -> URI (Session mode pooler, port 5432)"
    dashboard_config:
      - task: "Use existing Supabase project or create new one"
        location: "supabase.com -> your project"
  - service: netlify
    why: "Production hosting"
    env_vars: []
    dashboard_config:
      - task: "Create Netlify site linked to GitHub repo"
        location: "app.netlify.com -> Add new site -> Import an existing project"

must_haves:
  truths:
    - "Supabase PostgreSQL database has all tables from prisma/schema.prisma"
    - "Admin user is seeded in production database"
    - "Netlify site exists and is linked to the GitHub repo"
    - "All environment variables are configured in Netlify dashboard"
    - "App deploys successfully and is accessible via public URL"
  artifacts: []
  key_links:
    - from: "Netlify runtime"
      to: "Supabase PostgreSQL"
      via: "DATABASE_URL env var (session mode pooler)"
      pattern: "postgresql://.*pooler.supabase.com"
    - from: "Netlify build"
      to: "package.json postinstall"
      via: "npm lifecycle hook"
      pattern: "prisma generate"
---

<objective>
Provision the production database (Supabase), create the Netlify site, configure all environment variables, and get the first successful deployment live.

Purpose: This is the core deployment step. After this plan, the app is live and accessible at a public URL with a working database.
Output: Live app at a Netlify URL with production database connected.
</objective>

<execution_context>
@C:\Users\eryde\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\eryde\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-deployment/15-RESEARCH.md
@lib/env.ts
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Push schema to Supabase and seed admin user</name>
  <action>
**Step 1: Get connection strings from Supabase**
1. Go to your Supabase project dashboard
2. Navigate to Project Settings → Database → Connection string
3. Copy TWO connection strings:
   - **Session mode pooler** (port 5432, URL contains `pooler.supabase.com`): This goes in Netlify as DATABASE_URL
   - **Direct connection** (port 5432, URL contains `db.[ref].supabase.co`): Use this for running prisma commands locally

**Step 2: Push schema to production database**
Run this from the project root directory (replace with your DIRECT connection string):

```bash
DATABASE_URL="postgresql://postgres:[password]@db.[ref].supabase.co:5432/postgres" npx prisma db push
```

You should see output confirming all models were synced.

**Step 3: Seed admin user**
Run this to create the admin account in production:

```bash
DATABASE_URL="postgresql://postgres:[password]@db.[ref].supabase.co:5432/postgres" npx prisma db seed
```

Confirm the seed completed without errors.
  </action>
  <how-to-verify>
1. Supabase Table Editor shows tables matching prisma/schema.prisma
2. `prisma db push` output shows "Your database is now in sync with your Prisma schema"
3. `prisma db seed` completes without errors
  </how-to-verify>
  <resume-signal>Confirm schema pushed and seed succeeded, then type "database ready"</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Create Netlify site and configure all environment variables</name>
  <action>
**Step 1: Generate AUTH_SECRET**
Run locally:
```bash
openssl rand -base64 32
```
Copy the output. This is your AUTH_SECRET.

**Step 2: Create Netlify site**
1. Go to https://app.netlify.com -> "Add new site" -> "Import an existing project"
2. Connect to GitHub and select your repository (baseaimclientdashboard)
3. Build settings (should auto-detect):
   - Build command: `next build`
   - Publish directory: `.next`
4. **IMPORTANT: Do NOT click "Deploy site" yet** -- add environment variables first

**Step 3: Add environment variables**
In Netlify, go to Site configuration → Environment variables → Add a variable, and add ALL of the following:

| Variable | Value | Notes |
|----------|-------|-------|
| `DATABASE_URL` | Your Supabase session pooler connection string (port 5432, `pooler.supabase.com`) | From Task 1 |
| `AUTH_SECRET` | Output of `openssl rand -base64 32` | Generated in Step 1 |
| `NEXT_PUBLIC_APP_URL` | `https://your-site.netlify.app` | Use your Netlify URL for now; update later if adding custom domain |
| `STRIPE_SECRET_KEY` | Your live Stripe key (`sk_live_...`) | Stripe Dashboard -> Developers -> API keys -> Live mode |
| `STRIPE_WEBHOOK_SECRET` | `whsec_placeholder` | Temporary -- will be updated in Plan 03 after registering webhook |
| `RESEND_API_KEY` | Your Resend API key | Resend dashboard -> API Keys |
| `RESEND_FROM_EMAIL` | Your verified sender email | Optional -- only if you have a verified domain in Resend |
| `GOOGLE_OAUTH_CLIENT_ID` | Your Google OAuth client ID | Google Cloud Console -> APIs & Services -> Credentials |
| `GOOGLE_OAUTH_CLIENT_SECRET` | Your Google OAuth client secret | Same location as above |
| `GOOGLE_OAUTH_REFRESH_TOKEN` | Your Google OAuth refresh token | See Plan 03 Task 2 if you need to regenerate for production |
| `GOOGLE_DRIVE_ROOT_FOLDER_ID` | The Google Drive folder ID for client documents | From the Drive folder URL |
| `UPSTASH_REDIS_REST_URL` | Placeholder: leave empty for now | Will be configured in Plan 03 |
| `UPSTASH_REDIS_REST_TOKEN` | Placeholder: leave empty for now | Will be configured in Plan 03 |
| `SENTRY_DSN` | Placeholder: leave empty for now | Will be configured in Plan 03 |
| `NEXT_PUBLIC_SENTRY_DSN` | Placeholder: leave empty for now | Will be configured in Plan 03 |
| `SENTRY_ORG` | Placeholder: leave empty for now | Will be configured in Plan 03 |
| `SENTRY_PROJECT` | Placeholder: leave empty for now | Will be configured in Plan 03 |
| `SENTRY_AUTH_TOKEN` | Placeholder: leave empty for now | Will be configured in Plan 03 |
| `SKIP_ENV_VALIDATION` | `true` | **Temporary** -- allows first deploy without all services configured. Remove after Plan 03 completes. |

**IMPORTANT:** Set `SKIP_ENV_VALIDATION=true` for the first deploy so the build passes without Sentry/Upstash/Stripe webhook configured yet. We will remove this after Plan 03 configures all remaining services.

**Step 4: Deploy**
Click "Deploy site" in Netlify (or trigger a redeploy if it already deployed).

**Step 5: Verify deployment**
1. Wait for build to complete (check Netlify build log for errors)
2. Visit your Netlify URL (e.g., `https://your-site.netlify.app`)
3. You should see the login page
4. Try logging in with your admin credentials (from seed)
  </action>
  <how-to-verify>
1. Netlify build log shows deploy succeeded with no errors
2. Public URL loads the login page
3. Admin can log in with seeded credentials
  </how-to-verify>
  <resume-signal>Paste your live Netlify URL (e.g., https://xxx.netlify.app) and confirm admin login works, then type "deployed"</resume-signal>
</task>

</tasks>

<verification>
- Supabase database has all tables from schema.prisma
- Admin user can log in at the production URL
- Netlify deployment status is "Published"
- SKIP_ENV_VALIDATION is set to true (temporary, removed in Plan 03)
</verification>

<success_criteria>
App is live at a public Netlify URL. Database is provisioned with schema and admin user. All core env vars (DATABASE_URL, AUTH_SECRET, NEXT_PUBLIC_APP_URL, Stripe, Resend, Google Drive) are configured. SKIP_ENV_VALIDATION temporarily bypasses optional service validation. Covers DEPLOY-01 (app accessible via public URL).
</success_criteria>

<output>
After completion, create `.planning/phases/15-deployment/15-02-SUMMARY.md`
</output>
