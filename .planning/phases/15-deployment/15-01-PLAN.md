---
phase: 15-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json]
autonomous: true

must_haves:
  truths:
    - "package.json scripts section includes postinstall that runs prisma generate"
    - "Change is committed and pushed to main branch"
  artifacts:
    - path: "package.json"
      provides: "postinstall script for Vercel build"
      contains: "postinstall"
  key_links:
    - from: "package.json"
      to: "prisma/schema.prisma"
      via: "prisma generate command"
      pattern: "prisma generate"
---

<objective>
Add `postinstall: "prisma generate"` to package.json so Vercel generates a fresh Prisma Client binary on every deploy instead of using a stale cached version.

Purpose: Without this, Vercel's dependency cache serves a Prisma Client binary compiled for the wrong platform, causing PrismaClientInitializationError at runtime.
Output: Updated package.json committed and pushed to main.
</objective>

<execution_context>
@C:\Users\eryde\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\eryde\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-deployment/15-RESEARCH.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add postinstall script to package.json</name>
  <files>package.json</files>
  <action>
Add `"postinstall": "prisma generate"` to the `scripts` section of package.json. The scripts section should become:

```json
"scripts": {
  "dev": "next dev",
  "build": "next build",
  "start": "next start",
  "lint": "eslint",
  "postinstall": "prisma generate"
}
```

This ensures Vercel runs `prisma generate` after `npm install` during every build, producing a Prisma Client binary compatible with Vercel's Node.js runtime.

After editing, verify the JSON is valid by running: `node -e "require('./package.json')"`

Commit the change: `git add package.json && git commit -m "fix: add postinstall prisma generate for Vercel deployment"`

Push to main: `git push origin main`
  </action>
  <verify>
1. `node -e "const p = require('./package.json'); console.log(p.scripts.postinstall)"` outputs `prisma generate`
2. `git log --oneline -1` shows the postinstall commit
3. `git status` shows clean working tree on main branch
  </verify>
  <done>package.json has postinstall script, committed and pushed to main</done>
</task>

</tasks>

<verification>
- `grep -q "postinstall" package.json` exits 0
- `git log --oneline -1` shows postinstall commit
- `git diff origin/main` shows no diff (pushed successfully)
</verification>

<success_criteria>
package.json contains `"postinstall": "prisma generate"` in scripts, committed and pushed to main branch, ready for Vercel to pick up on first deploy.
</success_criteria>

<output>
After completion, create `.planning/phases/15-deployment/15-01-SUMMARY.md`
</output>
