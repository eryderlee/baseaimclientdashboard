---
phase: 23-demo-environment
plan: 03
type: execute
wave: 3
depends_on: ["23-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Demo admin (khan@baseaim.co) can log in and sees exactly 5 demo clients"
    - "Demo admin sees no real clients in client list or analytics"
    - "Real admin sees no demo clients in client list or analytics"
    - "2 in-setup clients show partial milestone progress at correct stages"
    - "3 post-setup clients show growth roadmap milestones on their dashboards"
    - "Post-setup client analytics pages show FB metrics (not empty/not-configured states)"
    - "Post-setup clients have invoices and documents visible in billing and documents pages"
    - "Dates, names, amounts, and metrics look plausible — no demo/test/fake indicators visible"
    - "Re-running npm run seed:demo does not break anything"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete demo environment end-to-end by running the seed command, logging in as the demo admin, and checking every page for realism and data isolation.

Purpose: This is the final quality gate for Phase 23. The seed script and DAL filtering were built in Plans 01-02 — this plan confirms everything works together as a cohesive demo experience with no visible indicators of fake data.

Output: Verified demo environment ready for sales demonstrations.
</objective>

<execution_context>
@C:\Users\eryde\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\eryde\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23-demo-environment/23-01-SUMMARY.md
@.planning/phases/23-demo-environment/23-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run seed and fix any runtime errors</name>
  <files>scripts/seed-demo.ts, prisma/schema.prisma</files>
  <action>
Run `npm run seed:demo` and verify it completes successfully. If there are any runtime errors (missing fields, type mismatches, unique constraint violations), fix them in the seed script or schema.

Then run `npm run seed:demo` a second time to confirm idempotency — no errors on re-run.

Then run `npm run seed:demo -- --clean` to verify cleanup works.

Then run `npm run seed:demo` one final time to recreate the demo data for verification.

Start the dev server with `npm run dev` for the verification checkpoint.
  </action>
  <verify>
All 4 seed commands exit 0. Console output shows expected summary and credentials.
  </verify>
  <done>Seed script runs successfully, is idempotent, and clean flag works.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete demo environment with 5 fake clients, isDemo isolation, static FB data, invoices, documents, and growth roadmap milestones</what-built>
  <how-to-verify>
1. Open http://localhost:3000/login
2. Log in as demo admin: khan@baseaim.co / BaseAim2026!
3. Verify admin dashboard shows exactly 5 clients — no real clients visible
4. Check that 2 clients show "Setting Up" badge and 3 show "Setup Complete" badge
5. Click into Hargrove & Associates (mature client) — verify:
   - 6 completed setup milestones + 7 growth milestones with review notes
   - Invoices tab shows 8 invoices with realistic amounts
6. Click "View as client" on Hargrove — verify:
   - Home page shows growth roadmap section (not setup phases)
   - Analytics page shows FB metrics (spend, leads, ROAS) — not "connect your ad account" prompts
   - Billing page shows invoices
   - Documents page shows documents
7. Exit preview, go back to admin dashboard
8. Log out and log in as the REAL admin (admin@baseaim.com / admin123 or your actual credentials)
9. Verify the real admin sees ZERO demo clients — no Calloway, Summit Ridge, Meridian, Apex, or Hargrove in the client list
10. Verify admin analytics page does not include demo data in revenue or FB metrics totals
  </how-to-verify>
  <resume-signal>Type "approved" if everything looks realistic, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. `npm run seed:demo` creates all demo data without errors
2. `npm run seed:demo` is idempotent on re-run
3. `npm run seed:demo -- --clean` removes all demo data
4. Demo admin sees only demo clients
5. Real admin sees zero demo clients
6. All demo client pages show realistic data with no fake/demo indicators
7. Post-setup clients have FB metrics, invoices, and documents
8. In-setup clients show correct milestone progress stages
</verification>

<success_criteria>
- Human verification confirms the demo environment looks fully realistic
- Data isolation is confirmed: demo admin sees only demo data, real admin sees only real data
- No visible indicators that any client is fake
</success_criteria>

<output>
After completion, create `.planning/phases/23-demo-environment/23-03-SUMMARY.md`
</output>
