---
phase: 14-enhanced-facebook-analytics-branded-reporting
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - components/dashboard/fb-ads-metrics.tsx
  - components/dashboard/fb-trend-chart.tsx
  - components/dashboard/fb-campaign-table.tsx
  - components/dashboard/fb-platform-split.tsx
  - app/dashboard/analytics/page.tsx
autonomous: true

must_haves:
  truths:
    - "Analytics page shows 12 metric cards including Reach, Frequency, Leads, Cost Per Lead, Outbound Clicks, Landing Page Views"
    - "Quality score pills display with emerald/amber/red colour coding, hidden when UNKNOWN"
    - "30-day spend + leads trend chart renders with real daily data using recharts ComposedChart"
    - "Top 5 campaign breakdown table shows campaign name, spend, impressions, leads"
    - "Platform split section shows Facebook/Instagram/etc. with spend and impressions per platform"
  artifacts:
    - path: "components/dashboard/fb-ads-metrics.tsx"
      provides: "12-card metric grid with quality pills"
      contains: "QualityPill"
    - path: "components/dashboard/fb-trend-chart.tsx"
      provides: "Dual-axis spend+leads trend chart"
      contains: "ComposedChart"
    - path: "components/dashboard/fb-campaign-table.tsx"
      provides: "Top 5 campaigns table"
      contains: "campaign_name"
    - path: "components/dashboard/fb-platform-split.tsx"
      provides: "Platform breakdown display"
      contains: "publisher_platform"
    - path: "app/dashboard/analytics/page.tsx"
      provides: "Server component fetching all new DAL data and passing to components"
      contains: "getClientFbCampaigns"
  key_links:
    - from: "app/dashboard/analytics/page.tsx"
      to: "lib/dal.ts"
      via: "import getClientFbCampaigns, getClientFbPlatformBreakdown, getClientFbDailyTrend"
      pattern: "import.*getClientFbCampaigns.*from.*dal"
    - from: "components/dashboard/fb-ads-metrics.tsx"
      to: "lib/facebook-ads.ts"
      via: "import FbInsights type with new fields"
      pattern: "import.*FbInsights.*from.*facebook-ads"
    - from: "components/dashboard/fb-trend-chart.tsx"
      to: "recharts"
      via: "ComposedChart with Bar + Line dual Y-axis"
      pattern: "import.*ComposedChart.*from.*recharts"
---

<objective>
Build the analytics UI: expand metric cards from 6 to 12, add quality score pills, add 30-day spend+leads trend chart, campaign breakdown table, and platform split section.

Purpose: Fulfills FBADS-EXT-01 through FBADS-EXT-04 — the full client-facing analytics view.
Output: Updated fb-ads-metrics.tsx (12 cards + pills), 3 new components (trend chart, campaign table, platform split), updated analytics page.tsx.
</objective>

<execution_context>
@C:\Users\eryde\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\eryde\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-enhanced-facebook-analytics-branded-reporting/14-RESEARCH.md
@.planning/phases/14-enhanced-facebook-analytics-branded-reporting/14-01-SUMMARY.md
@components/dashboard/fb-ads-metrics.tsx
@app/dashboard/analytics/page.tsx
@app/dashboard/analytics/export-buttons.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand fb-ads-metrics.tsx to 12 cards + quality pills</name>
  <files>components/dashboard/fb-ads-metrics.tsx</files>
  <action>
  Update the existing FbAdsMetrics component:

  1. **Update FbAdsMetricsProps** to accept the extended FbInsights type (no interface change needed — FbInsights itself was extended in Plan 01).

  2. **Add a QualityPill helper component** inside the file:
     - Props: `{ label: string; value?: string }`
     - Returns null when value is undefined or 'UNKNOWN' (Facebook requires 500+ impressions)
     - Color map using Tailwind classes:
       - ABOVE_AVERAGE: `bg-emerald-100 text-emerald-800`
       - AVERAGE: `bg-amber-100 text-amber-800`
       - BELOW_AVERAGE_35, BELOW_AVERAGE_20: `bg-red-100 text-red-800`
       - BELOW_AVERAGE_10: `bg-red-200 text-red-900`
     - Display text: "Above Avg", "Average", "Below Avg"
     - Pill style: `inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium`

  3. **Add helper functions** for the new metrics:
     - `getLeads(actions?: FbAction[]): number` — sums `getActionValue(actions, 'lead') + getActionValue(actions, 'offsite_conversion.fb_pixel_lead')`
     - `getCpl(spend: string, actions?: FbAction[]): string` — returns `formatCurrency(String(parseFloat(spend) / leads))` or `'--'` if leads === 0
     - `getOutboundClicks(outbound_clicks?: FbAction[]): number` — reduces array values, returns 0 if undefined
     - Import `getActionValue` from `@/lib/facebook-ads`

  4. **Expand the metrics grid from 6 to 12 cards.** Keep existing 6 cards (Ad Spend, Impressions, Clicks, CTR, CPC, CPM). Add 6 new cards:
     - **Reach** — `formatNumber(insights.reach)` — icon: Users (lucide-react) — subtitle: "Unique people reached"
     - **Frequency** — `parseFloat(insights.frequency).toFixed(2)` — icon: Repeat (lucide-react) — subtitle: "Average times seen per person"
     - **Leads** — `String(getLeads(insights.actions))` — icon: UserPlus (lucide-react) — subtitle: "Total lead conversions"
     - **Cost Per Lead** — `getCpl(insights.spend, insights.actions)` — icon: DollarSign — subtitle: "Average cost per lead"
     - **Outbound Clicks** — `formatNumber(String(getOutboundClicks(insights.outbound_clicks)))` — icon: ExternalLink (lucide-react) — subtitle: "Clicks to external site"
     - **Landing Page Views** — `formatNumber(String(getActionValue(insights.actions, 'landing_page_view')))` — icon: Globe (lucide-react) — subtitle: "Landing page loads (requires pixel)"

  5. **Add quality pills section** below the 12-card grid (inside the same fragment, after the grid div):
     ```tsx
     <div className="flex flex-wrap gap-2">
       <QualityPill label="Quality" value={insights.quality_ranking} />
       <QualityPill label="Engagement" value={insights.engagement_rate_ranking} />
       <QualityPill label="Conversion" value={insights.conversion_rate_ranking} />
     </div>
     ```
     This div renders nothing if all three are UNKNOWN (pills return null).

  6. **Update grid layout** to `grid-cols-2 md:grid-cols-3 lg:grid-cols-4` to accommodate 12 cards.

  IMPORTANT: landing_page_views is NOT a top-level field. Extract it from the actions array using `getActionValue(insights.actions, 'landing_page_view')`. outbound_clicks is FbAction[] — use reduce to sum values, do NOT parseFloat directly on the field.
  </action>
  <verify>Run `npx tsc --noEmit`. Visually inspect that 12 Card components exist in the grid. Grep for 'QualityPill' in the file.</verify>
  <done>FbAdsMetrics renders 12 metric cards in a responsive grid. Quality pills appear below cards when rankings are not UNKNOWN. Leads derived from actions array. Outbound clicks summed from FbAction array.</done>
</task>

<task type="auto">
  <name>Task 2: Create trend chart, campaign table, platform split components + wire analytics page</name>
  <files>
    components/dashboard/fb-trend-chart.tsx
    components/dashboard/fb-campaign-table.tsx
    components/dashboard/fb-platform-split.tsx
    app/dashboard/analytics/page.tsx
  </files>
  <action>
  **A. Create components/dashboard/fb-trend-chart.tsx** — 'use client' component:
  - Export `TrendDataPoint` type: `{ date: string; spend: number; leads: number }`
  - Export `FbTrendChart` component accepting `{ data: TrendDataPoint[] }`
  - Use recharts `ComposedChart` with:
    - `ResponsiveContainer` width="100%" height={300}
    - `CartesianGrid` strokeDasharray="3 3"
    - `XAxis` dataKey="date" tick={{ fontSize: 12 }}
    - Left `YAxis` yAxisId="spend" orientation="left" tickFormatter={(v) => `$${v}`}
    - Right `YAxis` yAxisId="leads" orientation="right" allowDecimals={false}
    - `Tooltip`
    - `Legend`
    - `Bar` yAxisId="spend" dataKey="spend" fill="#2563eb" name="Spend ($)" barSize={20}
    - `Line` yAxisId="leads" dataKey="leads" stroke="#10b981" dot={false} name="Leads" strokeWidth={2}
  - Show empty state message if data array is empty
  - Import `getActionValue` from `@/lib/facebook-ads` for the data transform helper
  - Export a `buildTrendData` helper function that transforms `FbDailyInsight[]` into `TrendDataPoint[]`:
    - date: `new Date(d.date_start).toLocaleDateString('en-US', { month: 'short', day: 'numeric' })`
    - spend: `parseFloat(d.spend)`
    - leads: `getActionValue(d.actions, 'lead') + getActionValue(d.actions, 'offsite_conversion.fb_pixel_lead')`

  **B. Create components/dashboard/fb-campaign-table.tsx** — 'use client' component:
  - Export `FbCampaignTable` accepting `{ campaigns: FbCampaignInsight[] }`
  - Import `FbCampaignInsight, getActionValue` from `@/lib/facebook-ads`
  - Render a styled table (not shadcn Table — just HTML table with Tailwind for simplicity):
    - Header row: Campaign, Spend, Impressions, Clicks, Leads
    - Body rows: campaign_name, formatCurrency(spend), formatNumber(impressions), formatNumber(clicks), leads (derived from getActionValue on actions array for 'lead' + 'offsite_conversion.fb_pixel_lead')
    - Style: `w-full text-sm`, header `bg-slate-50 text-left text-xs font-medium text-slate-500 uppercase`, rows `border-b border-slate-100`, cells `px-4 py-3`
  - Show "No campaign data" message if array is empty

  **C. Create components/dashboard/fb-platform-split.tsx** — 'use client' component:
  - Export `FbPlatformSplit` accepting `{ platforms: FbPlatformRow[] }`
  - Import `FbPlatformRow` from `@/lib/facebook-ads`
  - Display each platform as a row/card showing:
    - Platform name (capitalize first letter: "facebook" -> "Facebook")
    - Spend (formatCurrency), Impressions (formatNumber), Clicks (formatNumber)
  - Use a simple grid or list layout with platform name prominent
  - Calculate spend percentage per platform (platform spend / total spend * 100) and show as a percentage badge
  - Show "No platform data" if array is empty

  **D. Update app/dashboard/analytics/page.tsx:**
  1. Add imports for new DAL functions: `getClientFbCampaigns`, `getClientFbPlatformBreakdown`, `getClientFbDailyTrend` from `@/lib/dal`
  2. Add imports for new components: `FbTrendChart`, `buildTrendData` from `@/components/dashboard/fb-trend-chart`, `FbCampaignTable` from `@/components/dashboard/fb-campaign-table`, `FbPlatformSplit` from `@/components/dashboard/fb-platform-split`
  3. In the AnalyticsPage function, fetch new data in parallel with existing calls:
     ```typescript
     const [fbInsights, campaigns, platforms, dailyTrend] = await Promise.all([
       getClientFbInsights(datePreset),
       getClientFbCampaigns(datePreset),
       getClientFbPlatformBreakdown(datePreset),
       getClientFbDailyTrend(),
     ])
     ```
  4. Add new sections below the existing FbAdsMetrics component (inside the "Facebook Ads Performance" section):
     - **Spend & Leads Trend** — `<FbTrendChart data={dailyTrend ? buildTrendData(dailyTrend) : []} />` wrapped in a section with `<h3>` heading
     - **Top Campaigns** — `<FbCampaignTable campaigns={campaigns} />` wrapped in a section with `<h3>` heading
     - **Platform Breakdown** — `<FbPlatformSplit platforms={platforms} />` wrapped in a section with `<h3>` heading
  5. Only render the new sections when `isConfigured` is true (same guard as FbAdsMetrics)
  6. Pass campaigns and platforms data to ExportButtons (update the prop passing — Plan 03 will update ExportButtons itself, but the page needs to pass the data). Add `campaigns` and `platforms` props to the FbAdsMetrics component OR pass them directly in the page layout. Since ExportButtons is rendered inside FbAdsMetrics, the simplest approach is to pass campaigns and platforms as additional props to FbAdsMetrics, which will forward them to ExportButtons.
  7. Update FbAdsMetricsProps to accept optional `campaigns` and `platforms` arrays, and forward them to ExportButtons.
  </action>
  <verify>Run `npx tsc --noEmit`. Verify the 3 new component files exist. Verify page.tsx imports and renders all new components. Grep for `ComposedChart` in fb-trend-chart.tsx, `campaign_name` in fb-campaign-table.tsx, `publisher_platform` in fb-platform-split.tsx.</verify>
  <done>Analytics page renders 12 metric cards, quality pills, 30-day trend chart with dual Y-axes (spend bar + leads line), top 5 campaign table, and platform breakdown section. All data fetched in parallel from DAL. New components are properly typed client components.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. Analytics page shows 12 metric cards (6 existing + 6 new: Reach, Frequency, Leads, CPL, Outbound Clicks, Landing Page Views)
3. Quality pills render with emerald/amber/red colors, hidden when UNKNOWN
4. Trend chart uses recharts ComposedChart with Bar (spend) + Line (leads) and dual Y-axes
5. Campaign table shows top 5 campaigns with name, spend, impressions, clicks, leads
6. Platform split shows per-platform spend and impressions
7. All sections gracefully handle empty/null data
</verification>

<success_criteria>
- 12 metric cards visible in the analytics page grid
- Quality pills show color-coded ranking when available
- Trend chart renders with spend bars and leads line
- Campaign table lists up to 5 campaigns sorted by spend
- Platform breakdown shows distribution across Facebook/Instagram/etc.
- Page fetches all data in parallel (Promise.all)
</success_criteria>

<output>
After completion, create `.planning/phases/14-enhanced-facebook-analytics-branded-reporting/14-02-SUMMARY.md`
</output>
