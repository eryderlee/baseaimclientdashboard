---
phase: 18-complete-analytics-page-on-the-admin-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - components/admin/client-analytics-table.tsx
  - components/admin/admin-fb-trend-chart.tsx
  - app/admin/page.tsx
autonomous: true

must_haves:
  truths:
    - "Admin can see FB Spend (30d) and FB Leads (30d) columns in the client analytics table"
    - "Admin can see an aggregate 30-day spend + leads trend chart on the admin dashboard"
    - "Suspense fallbacks in admin page show styled skeletons instead of bare text"
    - "Trend chart streams independently via its own Suspense boundary"
  artifacts:
    - path: "components/admin/admin-fb-trend-chart.tsx"
      provides: "Client component rendering recharts ComposedChart for admin aggregate FB trend"
      min_lines: 30
    - path: "components/admin/client-analytics-table.tsx"
      provides: "FB Spend and FB Leads columns in client table"
      contains: "fbSpend"
    - path: "app/admin/page.tsx"
      provides: "AdminFbTrendSection async component with Suspense, fbPerClient data passed to table, styled fallbacks"
      contains: "AdminFbTrendSection"
  key_links:
    - from: "app/admin/page.tsx"
      to: "lib/dal.ts (getAdminFbPerClient)"
      via: "import and call in getAdminData"
      pattern: "getAdminFbPerClient"
    - from: "app/admin/page.tsx (AdminFbTrendSection)"
      to: "lib/dal.ts (getAdminFbDailyAggregation)"
      via: "async component calling DAL inside Suspense"
      pattern: "getAdminFbDailyAggregation"
    - from: "app/admin/page.tsx"
      to: "components/admin/admin-fb-trend-chart.tsx"
      via: "import and render inside AdminFbTrendSection"
      pattern: "AdminFbTrendChart"
    - from: "components/admin/client-analytics-table.tsx"
      to: "fbPerClient data"
      via: "props from page.tsx"
      pattern: "fbSpend.*fbLeads"
---

<objective>
Wire per-client FB data into the analytics table, add an aggregate FB trend chart with independent Suspense streaming, and replace bare-text Suspense fallbacks with styled skeletons.

Purpose: Closes the remaining 3 research gaps — admin sees per-client FB performance at a glance, gets a visual trend of aggregate ad spend, and sees polished loading states.
Output: Updated table with FB columns, new trend chart component, updated admin page with Suspense streaming and styled fallbacks.
</objective>

<execution_context>
@C:\Users\eryde\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\eryde\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-complete-analytics-page-on-the-admin-dashboard/18-01-SUMMARY.md
@lib/dal.ts
@app/admin/page.tsx
@components/admin/client-analytics-table.tsx
@components/dashboard/fb-trend-chart.tsx
@lib/facebook-ads.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FB columns to ClientAnalyticsTable and create AdminFbTrendChart</name>
  <files>components/admin/client-analytics-table.tsx, components/admin/admin-fb-trend-chart.tsx</files>
  <action>
**client-analytics-table.tsx changes:**

1. Extend the ClientData interface with two optional fields:
   ```
   fbSpend?: number | null
   fbLeads?: number | null
   ```

2. Add two new TableHead columns after "Next Due Date" and before "Actions":
   - `<TableHead className="hidden md:table-cell">FB Spend</TableHead>`
   - `<TableHead className="hidden md:table-cell">FB Leads</TableHead>`

3. Add matching TableCell entries in each row (also with `className="hidden md:table-cell"`):
   - FB Spend: `client.fbSpend != null ? '$' + client.fbSpend.toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 }) : '-'`
   - FB Leads: `client.fbLeads != null ? client.fbLeads.toString() : '-'`

The `hidden md:table-cell` classes hide these columns on mobile to prevent horizontal overflow.

**admin-fb-trend-chart.tsx (NEW FILE):**

Create a 'use client' component that renders a recharts ComposedChart for the admin aggregate trend. Follow the same pattern as `components/dashboard/fb-trend-chart.tsx` but simplified:

- Props: `data: Array<{ date: string; spend: number; leads: number }>`
- No range selector needed (data is always 30 days from the DAL)
- Format date labels with `new Date(d.date).toLocaleDateString('en-US', { month: 'short', day: 'numeric' })`
- Use ComposedChart with:
  - Bar for spend (fill="#2563eb", yAxisId="spend", barSize=20)
  - Line for leads (stroke="#10b981", yAxisId="leads", dot=false, strokeWidth=2)
  - CartesianGrid, XAxis, dual YAxis (left=$spend, right=leads count), Tooltip, Legend
- Wrap in ResponsiveContainer width="100%" height={300}
- Empty state: glass-card dashed border pattern with "No trend data available" message
- Import from 'recharts': ComposedChart, Bar, Line, XAxis, YAxis, CartesianGrid, Tooltip, Legend, ResponsiveContainer
  </action>
  <verify>Run `npx tsc --noEmit` to confirm no TypeScript errors. Check that both files exist and have the expected structure.</verify>
  <done>ClientAnalyticsTable has FB Spend and FB Leads columns that are responsive-hidden on mobile. AdminFbTrendChart renders a ComposedChart with spend bars and leads line.</done>
</task>

<task type="auto">
  <name>Task 2: Wire everything in admin page — FB data, trend Suspense, styled fallbacks</name>
  <files>app/admin/page.tsx</files>
  <action>
Update app/admin/page.tsx with three changes:

**1. Import and call getAdminFbPerClient in getAdminData():**
- Import `getAdminFbPerClient` and `getAdminFbDailyAggregation` from `@/lib/dal`
- Add `getAdminFbPerClient()` to the existing Promise.all in getAdminData (5th element)
- In processedClients map, look up each client.id in fbPerClient and add `fbSpend` and `fbLeads` to the returned object:
  ```
  const fbData = fbPerClient[client.id]
  return {
    ...existing fields,
    fbSpend: fbData?.spend ?? null,
    fbLeads: fbData?.leads ?? null,
  }
  ```

**2. Add AdminFbTrendSection async component with Suspense:**
- Create an async server component `AdminFbTrendSection` co-located in the same file (NOT exported, just a local function component)
- It calls `getAdminFbDailyAggregation()` and transforms the result using the date formatting from fb-trend-chart pattern
- Renders inside a Card with CardHeader "Ad Spend & Leads Trend (30d)" and CardContent containing the AdminFbTrendChart
- Import AdminFbTrendChart from `@/components/admin/admin-fb-trend-chart`
- Place it in the JSX between AnalyticsSummary and the Clients Table Card, wrapped in `<Suspense fallback={<AdminFbTrendSkeleton />}>`
- AdminFbTrendSkeleton: a Card with a Skeleton header line + a Skeleton rectangle (h-[300px] rounded) mimicking the chart area

**3. Replace bare-text Suspense fallbacks:**
- Replace `<div>Loading filters...</div>` with an inline skeleton: a div with 3 Skeleton elements in a flex row (search input skeleton w-64 h-10, select skeleton w-32 h-10, select skeleton w-32 h-10) — matching the ClientFilters shape
- Replace `<div>Loading clients...</div>` with an inline skeleton: a div with Skeleton rows (header row h-10 + 5 data rows h-12 each, all full-width) — matching the table shape

Import Skeleton from `@/components/ui/skeleton` and Card/CardHeader/CardTitle/CardContent if not already imported.
  </action>
  <verify>Run `npx tsc --noEmit` to confirm no TypeScript errors. Run `npm run build` to verify the page compiles and renders. Check that AdminFbTrendSection is wrapped in Suspense.</verify>
  <done>Admin page fetches per-client FB data and passes it to the table, renders an aggregate trend chart in its own Suspense boundary, and all Suspense fallbacks show styled skeletons instead of bare text.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `npm run build` succeeds
- admin page.tsx imports and calls getAdminFbPerClient (in parallel Promise.all)
- AdminFbTrendSection is an async component wrapped in Suspense
- ClientAnalyticsTable receives and renders fbSpend/fbLeads columns
- No bare `<div>Loading...</div>` text remains in admin page Suspense fallbacks
- FB columns are hidden on mobile (`hidden md:table-cell`)
</verification>

<success_criteria>
- Per-client FB Spend and FB Leads columns appear in the admin analytics table
- Aggregate 30-day trend chart renders with spend bars and leads line
- Trend chart streams independently (own Suspense boundary)
- All Suspense fallbacks show styled skeleton UI
- No TypeScript errors, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/18-complete-analytics-page-on-the-admin-dashboard/18-02-SUMMARY.md`
</output>
